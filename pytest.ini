[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    unit: Unit tests (no external dependencies)
    integration: Integration tests (require database/services mocked at boundary)
    e2e: End-to-end tests (require running infrastructure)
addopts = -v --tb=short
